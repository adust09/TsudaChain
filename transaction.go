
import(
"fmt"
"math/rand"
)

type accountInfo{
address := ""
value := 0
}

func main(){
address := new(accountInfo)
}

func makeTransaction(){
to := new(accountInfo)

if to.value == null
fmt.Print("to.address has no value")


}

func
